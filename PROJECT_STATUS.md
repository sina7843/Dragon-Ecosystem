# Dragon Ecosystem Project Status

Mark an item complete only after its implementation is reviewed, relevant checks pass, and the status change is included in the final commit for that item.

Sliced parents (`09`, `11`, `16`, `17`, `27`) are never executed as separate implementation prompts. Complete their `a`, `b`, and `c` slices in order.

## Foundation and Phase 1

- [x] DRAGON-00
- [x] DRAGON-01
- [x] DRAGON-02
- [x] DRAGON-03
- [x] DRAGON-04
- [x] DRAGON-05
- [x] DRAGON-06
- [x] DRAGON-07
- [x] DRAGON-08
- [x] DRAGON-09a
- [x] DRAGON-09b
- [x] DRAGON-09c
- [x] DRAGON-10
- [x] DRAGON-11a
- [x] DRAGON-11b
- [x] DRAGON-11c
- [x] DRAGON-12
- [x] DRAGON-13
- [x] DRAGON-14
- [x] DRAGON-15
- [x] DRAGON-16a
- [x] DRAGON-16b
- [x] DRAGON-16c
- [x] DRAGON-17a
- [x] DRAGON-17b
- [x] DRAGON-17c

## Later phases

- [x] DRAGON-18
- [x] DRAGON-19
- [x] DRAGON-20
- [x] DRAGON-21
- [x] DRAGON-22
- [x] DRAGON-23
- [x] DRAGON-24
- [x] DRAGON-25
- [x] DRAGON-26
- [x] DRAGON-27a
- [x] DRAGON-27b
- [x] DRAGON-27c

## Current work

- Active item: `DRAGON-29F` (support surface and help centre). **PAGE-023 → Partial. 4 → 3 pending**, and the three that remain are each blocked on a non-engineering input. Verified the prior audit's three claims before changing anything — all three held: the support API exists, no user-facing surface uses it, and no approved FAQ copy exists anywhere in the repository. Built the clause that was engineering-owned: `/{locale}/help`, lazy-loaded, linked from the public and account navigation, offering the support entry point and the caller's own cases. Two boundaries were closed on the way, both found by reading the code rather than the row. **The participant projection was leaking operator-only fields** — `assignedTo` (the staff account handling the case) and `resolutionNote` (the operator's working text, written for staff) were returned by the participant-facing `GET /support/cases` and `/support/cases/:id`; they are now confined to a separate `operatorSupportView` used only by the `support.manage` routes, which is the same boundary registrations has drawn around `decidedBy` since DRAGON-08. **The intake category was an unbounded 64-character string**, so a case could be filed under a value no locale could ever label; it is now the three categories the operator console already ships approved fa/en wording for, and the requester sees that same vocabulary rather than a second invented one. **What was deliberately not built: the FAQ, the tournament help, and the search over them.** No approved copy exists, so none was written — no invented answer, no contact address, no telephone number, no response-time or availability promise — and a browser assertion fails if any of those appear on the page. The row says so instead of the page implying otherwise. 7 route-level integration tests (authentication, the ownership boundary through a case id, operator-note absence in both the detail and the list, the closed vocabulary and length bounds, the per-requester rate limit, cursor paging with no repeat or drop) and 33 browser tests across fa/en at 320px/375px/1440px. Both the privacy assertion and the no-promise assertion were proved to have teeth by temporarily reinstating the defect. Full browser suite because the navigation changed: **536 passed, 1 skipped, 0 failed**; two earlier runs failed under host contention and are recorded in `IMPLEMENTATION_STATUS.md` with the reasoning that rules this slice out, rather than dismissed as flaky. **Ecosystem verdict unchanged: NO-GO** — one clause of one page moving to Partial changes nothing about release readiness. No commit created.
- Previous item: `DRAGON-29E` (registration history and participant views). **6 → 4 pending.** Verified the prior audit's four findings against the code before changing anything — all four held. Added an **append-only registration transition history**: `registration_transitions` (migration `032-registration-history`) written inside the same unit of work as every state change, from the one `#audit` helper all four transition paths already route through, so the history cannot drift from the audit trail or miss a path. Because the registration write is version-guarded, the guarantees fall out rather than being bolted on — current state and the latest entry always agree, an invalid transition or an aborted transaction appends nothing, and three concurrent decisions produce one winner and one entry (all asserted). **Legacy registrations are not backfilled**: they keep their state and report `historyAvailable: false`, because inventing an actor, a reason or a timestamp for transitions nobody captured would manufacture evidence. Added `GET /me/tournament-registrations` (+ `/:id` with history) and `GET /me/matches`, both deriving ownership from the session — no account id is accepted anywhere, and a foreign registration id answers exactly as a missing one, so an IDOR probe learns nothing. New index `registration_account_created` `{accountId, createdAt:-1, _id:-1}` serves the newest-first list that `registration_account` `{accountId, tournamentId}` could not. Two pages at `/{locale}/account/registrations` and `/account/matches`, in nav, both locales, RTL/LTR, 320px overflow asserted, with loading, empty, error and unavailable-reference states. **The privacy boundary was the main design constraint**: `statusView` has always excluded the staff `reason`, so the transition record never stores it and the participant view carries an actor *role*, not an identity; likewise DRAGON-29D's reschedule reason stays server-side while the participant sees that the time moved and what it was — asserted in the browser by checking the reason text is absent from the page. **PAGE-018 → Implemented. PAGE-017 → Partial**, naming three unsatisfied clauses honestly: the staff reason (privacy boundary), payment detail beyond the `pending_payment` state, and the withdraw action, which exists and is authorized but is offered on the tournament page rather than here. Full E2E run because the account navigation changed: **503 passed, 1 skipped**. **Ecosystem verdict unchanged: NO-GO.** No commit created.
- Previous item: `DRAGON-29D` (final eight evidence-pending requirements). **8 → 6.** Read all eight against `Requirements.md` rather than their traceability notes, which surfaced that the previous framing was wrong in one place and incomplete in another. **Match scheduling was not "a reschedule operation missing" — matches had no time field at all**, so TOURN-019 ("match schedules MUST use UTC storage") was rowed Implemented on the display-side formatters while there was nothing to store. Implemented the capability end to end: `scheduledAt` on the match, migration `031-match-scheduling` (additive, idempotent, backfills only the absence — no invented time, no fabricated history), an append-only `competition_match_schedules` revision trail with a unique `(matchId, revisionNumber)` index as the integrity authority, and `PATCH /admin/tournaments/:id/matches/:mid/schedule` gated by `tournament.manage` scoped to the tournament. The write parses and re-serializes to UTC, so `18:30+03:30` stores as `15:00Z` rather than an organizer's wall clock; an optimistic version guard makes exactly one of N concurrent attempts win and doubles as the idempotency boundary, so a retry appends no second revision and publishes no duplicate event; `completed`/`bye` matches and locked competitions are refused, the lock re-checked inside the transaction. One `competition.match_rescheduled` event per participant account, resolved from the authoritative registration records and mapped to a new `match_rescheduled` template with fa/en strings — **the operator reason is deliberately excluded from the participant payload** because it is staff-facing and may name a third party. 13 integration tests; also added the guard that was missing entirely: nothing asserted that a notification template key has a localized string, because the inbox renders a *computed* key the literal-key guard cannot see. **API-043 and TOURN-020 → Implemented; TOURN-019 corrected with real evidence; PAGE-050's note corrected** (its scheduling clause is now UI-only). **PAGE-017, PAGE-018, PAGE-023 remain pending** as engineering work with verified next actions — PAGE-018's blocker is now removed at the data level. **OPS-008, PAGE-025, PAGE-051 remain pending for a reason worth naming: each is blocked by a policy input that has no registered open-decision id**, so they cannot honestly be rowed Blocked (OD ids are defined in the protected `Requirements.md`, OD-003…OD-030, and inventing OD-031 would fabricate an external decision). OPS-008 was deliberately not implemented: the requirement defines localization but not fail-open-versus-fail-closed, affected or exempt routes, activation authority, audit or recovery — choosing those by assumption would decide whether a misread config takes the site down and whether a degraded system grants privileged access. **Ecosystem verdict unchanged: NO-GO.** No commit created.
- Previous item: `DRAGON-29C` (remaining evidence-pending closure). Recalculated the pending count from the committed file (**27**, matching the last report) and reviewed all 27 rows individually against their authoritative requirement rather than their ID or existing wording. **27 → 8.** Eight rows became **Implemented** by writing six documents against the code as it actually is — `DOMAIN_EVENTS.md` (DOC-009, EVENT-012: every event with producer, payload and consumer, the envelope, the outbox guarantee, and the full event→notification mapping), `COMPETITION_GUIDE.md` (DOC-011: formats, bye semantics and pre-advancement, deterministic standings, per-format tiebreaks, what OD-006 gates), `FINANCIAL_GUIDE.md` (DOC-012: Money contract, six account types, five transaction types, unique-index integrity, hold purposes, the fully-gated transfer boundaries, refunds documented as deliberately absent), `LOCALIZATION_GUIDE.md` (DOC-013 + DOC-014, the latter a ten-step add-a-locale sequence naming every file and the two consequences code cannot cover), `AUTHORING_GUIDE.md` (DOC-015), `METRICS.md` (ANALYTICS-008: every metric with its exact query and single calculation site). Two became **Partial**: PERF-009 — cache headers implemented (`/assets/` `immutable` for a year because the build hash *is* the invalidation mechanism, `index.html` `no-cache` so a deploy is picked up), guarded by a new `nginx-cache.test.ts` that also asserts the per-location `add_header` replacement does not silently drop the security headers, with **no CDN** as the named unsatisfied clause; and ADMIN-011 — per-user role reads and revocation exist, **no cross-user report and no last-use record**. Nine became **Blocked by open decision**, which is the correction that matters most: the previous revision claimed *"no external input is needed to build any of them"* and that was **wrong** for email identity (OD-003), deletion/consent/legal (DEC-043) and analytics reporting (OD-026). **Eight rows remain pending, deliberately** — all engineering-owned, each now naming its missing evidence, why existing evidence is insufficient, its owner and a concrete next action. Two were buildable and deliberately not built: `/status` over `/health/ready` alone would say nothing useful during the outage it exists for, and `/help` would ship copy nobody approved. PAGE-017 additionally needs a new index and migration, and the registration record stores no state history, so its "status history" clause cannot be met from it. No product behaviour changed beyond the nginx cache policy; no decision resolved; no page invented to clear a row. **Ecosystem verdict unchanged: NO-GO.** No commit created.
- Previous item: `DRAGON-29B.2` (commit and validate performance evidence). Closed the mismatch where release documents cited `apps/api/src/perf/perf.itest.ts` — a file that had **never been committed**, so PERF-004 and PERF-014 cited evidence absent from Git and CI, and the integration count read 501 locally against 493 remotely. Audited the directory by content rather than by history: **one source file, 489 lines, no generated output, no benchmark artefacts, no secrets, no machine-specific paths, nothing written to disk.** It is suitable for source control and is retained (option 1). Isolation verified: `createTestDatabase()` gives each run its own `dragon_test_<uuid>` dropped on dispose, every fixture id is counter-derived and unique, and `tsconfig.build.json` already excludes `*.itest.ts` so no test code reaches the production image. **No latency threshold is asserted anywhere** — all eight tests assert correctness invariants only (one key moves value exactly once with one ledger posting; eight distinct keys all accepted, which is the DRAGON-28 regression guard; 24 claims against a budget of 20 admit exactly 20 with every refusal compensated; eight buyers for one unit yield exactly one winner, stock 0, one order line; the stale scan is IXSCAN-served and honours its bound), so a slower CI runner cannot fail the job. Two hygiene fixes: the run banner printed the Mongo connection string, which may carry credentials into a CI log — it now prints host and port only; and the shared ledger handle is documented rather than left looking like a bug. Added `npm run test:performance` as a bounded focused workflow **without** removing the tests from the integration job. Determinism: 5 bounded repetitions, 8/8 passed each time, IXSCAN every run. Integration count **493 → 501 in CI** once committed, matching local. Documentation corrected where it claimed the file was untracked or miscounted the tests; **no release claim strengthened** — the timing figures stay *measured locally*, the invariants are now *structurally verified in CI*, production-scale load stays *not measured*, PERF-001/002/003/014 and OPS-014 stay **Blocked** on OD-023/DEC-046, and the **ecosystem verdict remains NO-GO**. No commit created.
- Previous item: `DRAGON-29B.1` (first remote CI failure remediation). The pipeline ran for real. Six jobs passed (`validate`, `static`, `unit`, `build-budget`, `integration`, `migrations`); `security`, `persistence`, and `e2e` failed, and **all three were genuine** — no pipeline redesign was needed and nothing was fixed by weakening a check. **security:** `find-my-way <=9.6.0` (GHSA-c96f-x56v-gq3h, CVSS 7.5, HTTP/2 denial of service) sat in the shipped tree; updated to `9.7.0` inside Fastify's existing `^9.6.0` range — lockfile only, no manifest or API change, audit now reports 0 vulnerabilities, threshold unchanged. **persistence:** my own assertion was wrong, not the repository — a step required `WEB_PORT=18080` to move the published port, which is behaviour of the *uncommitted* `docker-compose.yml` change; CI checked out committed state (`8080:8080` hardcoded) and failed in 14 seconds before `verify:persistence` ran. The step now asserts the committed contract — web publishes exactly `8080:8080`, `api` and `mongo` publish nothing — which is stricter about what matters. **e2e:** 8 failures exposed **two real product defects** hidden behind three assertions that passed on their first poll during a loading window, which is why the local suite never saw them: (1) the public player page never read the viewer's follow state, so after any reload the toggle read "Follow" for someone already following and pressing it followed again — *unfollowing from that page was impossible*, confirmed by a forced local trace showing `POST /follows` 201 and never a `DELETE`; fixed by returning `viewerFollows` from `getPublicProfile`, which already had the viewer's id and already queried the follows collection; (2) `AdminStoreView` decided its permission gate from `/store/config` and `/products` — both public endpoints answering 200 for anyone — so the operator console rendered in full for any signed-in user; fixed by probing `store.manage` through the existing `useAdmin()` capability probe. The server was never the hole in either case. Each strengthened assertion was proven to have teeth by temporarily reinstating the defect and confirming the test fails. `E2E_WORKERS` left at 2 — the run showed no contention symptom. Local verification all green including the full browser suite (464 passed, 1 skipped) and 501 integration tests. **A second remote run is required before the pipeline may be called green; none is claimed.** No commit created.
- Previous item: `DRAGON-29B` (production-grade CI pipeline). The repository had **no CI configuration of any kind**; it now has `.github/workflows/ci.yml` and `CI.md`. GitHub Actions was chosen on the repository's own evidence rather than because it uses Git: `REQUIREMENTS_TRACEABILITY.md` names `.github/workflows` as the specific missing artefact in SEC-016, SEC-017, and TEST-026. Ten jobs on `pull_request` and `push` to `main` plus manual dispatch — `validate`, `static`, `unit`, `integration`, `build-budget`, `migrations`, `e2e`, `security`, `persistence` (main and manual only), and a `required` summary that fails when any of them fails, is cancelled, or is skipped for a reason it did not declare. Every step runs a script the repository already owns, so a green pipeline means the same commands a developer runs locally passed; `permissions` is `contents: read` and the pipeline uses **no secrets at all**, so an untrusted forked pull request has nothing to reach. New supporting scripts: `ci-validate.mjs` (lockfile, toolchain Node floor, CI-referenced scripts exist, no `|| true` in a verification script, duplicate migration ids, tracked key material) and `verify-migrations.mjs` (fresh database → all 30 migrations apply, `030-recovery-indexes` and its indexes present, second pass applies nothing; existing database → immutable ledger, order, audit, and enrolment records survive a further pass byte for byte and stay queryable). Local `npm run ci` (no Docker) and `npm run ci:full` mirror it. **Two findings this pipeline surfaced rather than hid:** the toolchain needs Node ≥22.18.0 for unflagged TypeScript execution while `engines.node` says `>=22.12.0` — right for the container, which runs compiled JavaScript — so `.nvmrc` now carries the toolchain version and `ci:validate` enforces it; and `ci / security` **fails on the current lockfile** because `find-my-way <=9.6.0` (GHSA-c96f-x56v-gq3h, CVSS 7.5, HTTP/2 denial of service) is in the shipped dependency tree. That is the gate working. It was not fixed here — this slice is barred from applying dependency updates — and the alternative, a formal risk acceptance on the grounds that the API runs HTTP/1.1 behind nginx, needs the named approver SEC-017 requires and the repository does not have. **The workflow has never executed:** no Git remote is configured. Branch protection is likewise not active. Classification: **CI implementation complete, repository administrator activation pending.** SEC-016 and SEC-017 moved from `Evidence pending` to `Partial`; TEST-026 stays `Partial` with its unsatisfied clauses rewritten. **Ecosystem verdict unchanged: NO-GO.** No commit created.
- Previous item: `DRAGON-29A` (browser E2E stabilization). The instability DRAGON-28 opened as an engineering blocker with no established cause is **diagnosed and closed**. Its "all on small-mobile" reading was an artefact of project order — small-mobile runs first and absorbs the start-up contention; a deliberate full parallel reproduction produced 7 failures across all three viewport projects, which made the causes separable. Three root causes, none of them a product defect: (1) `teams.spec.ts:58` was never intermittent — it failed in all three projects at once because it waited on `expect(toast).toHaveCount(1)` after sending an invitation, and the toast queue is module state that survives client-side navigation, so the *"team created"* toast pushed moments earlier satisfied the count instantly and the invitee loaded their page while the invitation was still being written; (2) the "disposable" e2e database had never been disposed of and held **22,597 accounts**, against generators drawing `0912` + seven random digits, so roughly 800 sign-ins per run silently reused an existing account about twice; (3) the rest was host CPU saturation — two failures were `Test timeout of 30000ms exceeded while setting up "page"` and the retained traces show the application mid-request when the assertion budget expired (in one, the API answered `202` in 288 ms and the renderer had not run the continuation five seconds later). Fixed by waiting on the real API response instead of a toast, collision-free shared generators, a database reset before each run, readiness (`/health/ready`) rather than liveness as the web-server gate, artifact retention that actually produces traces (`retain-on-failure` had been paired with `retries: 0`, so none was ever written), and a worker count matched to journeys that run two or three pages each. The worker cap is load-bearing, not cosmetic: a control run at the old count with every other fix applied still failed 4 tests, all four the 30-second page-setup timeout. Evidence: `npm run e2e` 464 passed/1 skipped/0 failed exit 0; a second full run without the database reset identical; small-mobile alone 155 passed exit 0; focused repetition ×3 over the five affected specs 297 passed exit 0. No product code changed. No commit created.
- Previous item: `DRAGON-28` (release remediation and evidence closure). Five engineering gaps closed: the economy concurrency failure was diagnosed as a **real product defect** and fixed (the transfer-window claim treated "the window did not exist yet" as "the budget is exhausted", refusing a legitimate transfer with a misleading limit error whenever two arrived together on a cold window) — ~1-in-3 failures before, 0 in 10 bounded runs after; Docker engine and health restored; persistence PASS at the current commit; entry bundle **376.27 → 341.13 kB** against an unchanged 380 kB budget by lazy-loading the three public detail views; and a full browser run obtained. That run also **corrected an earlier misreport of mine**: the browser suite never "exited non-zero while all tests passed" — those runs had a genuinely failing test (`moderation.spec.ts`, "report a tournament … and a moderator sees it (fa)") that my console filters truncated out of view. It is an ordinary flake, still undiagnosed. Continued: the moderation browser flake is now **diagnosed** — a test-isolation defect (the queue's 20-row first page fills with other specs' cases during a full run), 6/6 clean in isolation, remedy named but not applied because it needs product scope; and `ACCESSIBILITY_CERTIFICATION.md` was added as engineering preparation with human certification pending. All **344** `Evidence pending` traceability rows were then dispositioned row by row — 200 became Implemented, 96 Partial with the unsatisfied clause named, 29 stay pending because nothing is built and engineering owns them. Bounded local performance measurements were added (`apps/api/src/perf/perf.itest.ts`, five contention scenario groups totalling eight tests, zero errors), and the moderation flake was fixed test-only — the spec pages forward through the queue's own control, no assertion weakened; the moderation test passed in all **17** full-suite runs. Those runs also exposed a *separate* problem: only 14 of 17 were clean, with three unrelated small-mobile specs each failing once for reasons not yet established — recorded as a new engineering risk rather than smoothed over. **Correction:** an earlier claim that every remaining blocker is external was wrong; engineering still owns the 29 pending rows, the 125 partial rows, and the absence of any CI pipeline. **Ecosystem verdict remains NO-GO.**
- Previous item: `DRAGON-27c` (final ecosystem release evidence and decision). **Final ecosystem verdict: NO-GO**, recorded in `RELEASE_DECISION_ECOSYSTEM.md`. Four phase decisions remain NO-GO, Phase 1 forbids production deployment, ten external decisions are open, no live payment provider exists, and authorized human sign-off is outstanding everywhere. No implementation failure drives the verdict. Two evidence gaps are recorded rather than papered over: the browser suite could not be run this session because the Docker engine began returning 500 mid-slice, so no full browser run exists against the 27A/27B commits; and a money-path concurrency test in `economy.itest.ts` failed once in four full-suite runs and could not be diagnosed before the environment went down — it is listed as a release blocker for the economy scope, not as a flake.
- Previous item: `DRAGON-27b` (security, finance, data-integrity, and failure-recovery audit). Six confirmed defects fixed, including a transfer-window concurrency bug that admitted an extra transfer past its limit and a missing enrolment index that made the recovery scan a collection scan. Added a shared read-only stuck-reservation detector — detection only; the remedy stays policy-blocked under DEC-034.
- Previous item: `DRAGON-27a` (cross-phase integration audit). One confirmed defect: `t('tournaments.tbd')` resolved in neither locale and rendered as a raw key. Fixed, plus a guardrail asserting every literal `t()` key resolves in every locale — the existing parity test could never have caught a key missing from both files.
- Previous item: `DRAGON-26` (Phase 5 commerce and economy release closure). Phase 5 traceability completed and corrected: twelve rows that had sat "Evidence pending" since DRAGON-17a are now evidenced against the code that satisfies them (PAYOUT-001/002/003, REWARD-001/008, DATA-070/071, PAGE-058/059, ROLE-022, BR-022, JOURNEY-008), and eight requirements that had no row at all were added (GOAL-009, UC-020/021, JOURNEY-007, DATA-072, API-095/096, PAGE-042). Four deviations are recorded rather than hidden: a payout is modelled as the prize entitlement record with `/admin/entitlements/…` paths, there is no mixed-payment order, prize entitlements live on the wallet page, and `/account/payouts` is not built. **Phase 5 release decision: NO-GO**, recorded in `RELEASE_DECISION_PHASE5.md` — blocked entirely by OD-019 (physical commerce), OD-020 (affected digital products), and OD-030 (peer commerce), with **no implementation failure outstanding**; the two Critical and one High findings from the DRAGON-25 review are fixed and regression-tested. Docker healthy, Mongo persistence PASS, lint clean.
- Previous item: `DRAGON-25` (Phase 5 economy, rewards, peer transfer, and payouts). A new `economy` module (migration `029-economy`) with reward rules and grants, direct user-to-user Dragon Coin transfer, atomic rolling-window limits with a manual-review hold, and a ledger reconciliation report; plus payout hardening in `prizes`: the full nine-state lifecycle, actor-level dual control, recipient verification before settlement, retry on the same record, reversal that preserves the original evidence, and a report reconciling definition → allocation → ledger → settlement. **Cash redemption, sell-back, an exchange rate, and order-book trading are absent, not gated** — proven three ways, including that the ledger has no transaction type that could balance a cash-out. OD-030 keeps peer *commerce* out while plain transfer, which DEC-022/023 approve, is enabled. The security review returned **REQUEST-CHANGES with two Critical and one High**; all three were real, are fixed, and each has a regression test: a dual-control bypass reachable by failing then retrying a never-approved payout, a double ledger posting when two requests raced under one idempotency key, and rolling-window limits that were read-then-decided rather than claimed. PAGE-043 (`/account/payouts`) is deliberately not built.
- Previous item: `DRAGON-24` (Phase 5 store catalog, inventory, and fulfillment). A `store` module (migration `028-store`) delivering the localized physical/digital catalog with variants and SKUs, auditable internal stock, versioned discounts, a versioned cart, idempotent checkout, immutable order snapshots, internal fulfillment states, digital entitlements, and a financial reconciliation report. Settlement is Dragon Coin through the shared holds boundary — the store never posts to the ledger itself, which is the ROLE-021 boundary. Stock is claimed by a conditional `$inc` inside the write transaction, so two buyers racing for the last unit produce exactly one order; a payment that cannot be funded returns the stock and grants nothing. **Both open decisions stay closed:** OD-019 keeps physical items authorable and browsable but **not purchasable** (`PHYSICAL_FULFILLMENT_ENABLED=false`) because selling one commits us to a delivery with no approved carrier, region, rate, or service level, and no carrier concept exists in the code at all; OD-020 keeps entitlement revocation absent — no route, no state. There is no returns, refund, or RMA surface anywhere (COMMERCE-010, DEC-034). Prize and payout requirements (PAYOUT-001..012, API-095/096) are **not** in this slice. One known limitation is recorded rather than hidden: a process crash between the order commit and the coin capture leaves stock claimed on a stuck order — a shared characteristic of the existing holds integration that needs one sweeper across all purchase flows.
- Previous item: `DRAGON-23` (Phase 4 community release closure). Phase 4 traceability corrected and evidenced: several DRAGON-22 rows had been mapped to the wrong requirement text (SOCIAL-011 is per-channel notification preferences, not privacy-by-default; MOD-008 is the appeal gate, not the report workflow; NOTIF-011 is push, not mentions) and now say what the requirement actually says. Closed three evidence gaps: rendered post bodies and composers carry `dir="auto"` so a Persian post inside the English feed keeps its own direction (SOCIAL-004), guardrails prove community content has no leak path through global search, and the JOURNEY-006 loop — discover, follow, publish, react, comment, unfollow — runs end to end in fa and en. Two path deviations are recorded rather than hidden: the report intake is `/social/reports` (the requirement's `/reports` is already taken by the moderation module, and colliding crashes startup), and the social profile lives on the existing `/players/{username}` page because SOCIAL-001 requires existing profile URLs to stay valid. **Phase 4 release decision: NO-GO**, recorded in `RELEASE_DECISION_PHASE4.md` — blocked entirely by OD-017 (blocking, muting, privacy defaults), OD-024 (appeals), and OD-027 (push), with **no implementation failure outstanding**. Docker healthy, Mongo persistence PASS, lint clean.
- Previous item: `DRAGON-22` (Phase 4 community and advanced team roles). Follow relationships for users, teams, and games; posts, comments, and reactions; a visibility-aware feed; reporting into the shared moderation case workflow; a community moderation console; and social profiles whose statistics state their own source. Visibility is decided on every read (BR-025) — nothing is fanned out, so unfollowing or narrowing a profile takes effect on the next request with no rebuild. Advanced team roles (`manager`, `captain`) are two new values of the existing per-membership `role` field, so **no team migration exists** and every Phase 1 team id, membership id, and roster snapshot survives untouched. **All three open decisions stay closed:** OD-017 keeps blocking and muting *absent* rather than merely disabled (`SOCIAL_BLOCKING_ENABLED=false`, guardrail-tested against the route registry and the module's own collections), OD-024 keeps appeals off, and OD-027 keeps push out of the notification channel map. ANALYTICS-005 (community analytics) is deferred and rowed as such. Two real defects were found by the new tests and fixed in the product rather than the test: an over-long post body was silently truncated, and `POST /reports` collided with the moderation module's existing route (a startup crash) — a route-registry guardrail now covers every module.
- Previous item: `DRAGON-21` (Phase 3 education release closure). Phase 3 hardened and evidenced: the paid course journey now runs end to end in fa and en (buy Dragon Coin through the mock provider, reserve, capture, reach the lessons), with payment failure, a replayed provider callback, access denial, progress persistence, and completion each covered. Course completion publishes through the shared notifications outbox; a course capture is reconciled against the shared ledger; the player is verified at the 320px floor with a real navigation landmark; Mongo persistence passes. **Phase 3 release decision: NO-GO**, recorded in `RELEASE_DECISION_PHASE3.md` — blocked entirely by OD-015 (ownership, refund, revocation, coach commercial terms) and OD-016 (quiz/exercise scope), with **no implementation failure outstanding**.
- Previous item: `DRAGON-20` (Phase 3 courses, enrolment, and progress). Course authoring with a five-state lifecycle and publication completeness checks, explicit lesson ordering with prerequisite locks, free and paid enrolment with an entitlement link, monotonic per-lesson progress, deterministic completion, moderated reviews, and approved-fields-only coach profiles. Learner catalog, course detail, player, and an education console, all bilingual. **Both open decisions stay closed:** OD-015 keeps paid enrolment gated off (`PAID_COURSES_ENABLED=false`) and a Toman price refused; OD-016 keeps quiz and exercise lesson types refused. PAGE-033 (coach page) and ANALYTICS-004 (course revenue reports) are deferred behind OD-015 and rowed as such.
- Previous item: `DRAGON-19` (Phase 2 live chat, moderation, and release). Moderated live chat delivered end to end: rooms linked to streams with a moderation scope, totally-ordered messages over a per-room sequence, server-side duplicate protection, rate-limited sending with backpressure, room-scoped timeouts and bans, message removal with retained evidence, and reports that open shared moderation cases. **Phase 2 release decision: NO-GO**, recorded in `RELEASE_DECISION_PHASE2.md` — blocked entirely by external decisions (OD-013 contracted Arvan capabilities, OD-014 rights/takedown/archive policy, INT-004 credentials), with **no implementation failure outstanding**. Phase 2 parent remains open pending those decisions and authorized human sign-off.
- Previous item: `DRAGON-18` (Phase 2 stream catalog and Arvan adapter). Stream module delivered end to end: Dragon-owned stream identity/schedule/relationships/access policy, the full seven-state lifecycle, a provider adapter boundary with a deterministic stub, idempotent provisioning, operator reconciliation, degraded-state handling with operator alerts, rights confirmation before scheduling, and localized fa/en discovery, watch, and operations pages. **Both open decisions stay closed:** OD-013 keeps every Arvan-specific behaviour out (`STREAMING_PROVIDER=arvan` fails startup), and OD-014 keeps archive publication and takedown refused (`STREAM_RIGHTS_POLICY_APPROVED=false`). STREAM-010 (highlights) and STREAM-012 (provider playback analytics) are deferred behind those gates and rowed as such. Live chat (CHAT-*) is DRAGON-19 and was not started.
- Previous item: `DRAGON-17c` (Phase 1 release decision + acceptance closure) — decision recorded in `RELEASE_DECISION.md`, validated by `npm run decision:check` (12/12). **Decision: GO WITH CONDITIONS** for the local/test release candidate at `09b1af5` (mock-provider, in-app notifications, free + gated-paid flows, fa/en, 3 viewports, ≤1,000 capacity). **Production deployment NOT authorized. Final Phase 1 acceptance withheld** — 362 `Evidence pending` rows undispositioned (no repository-authorized waiver exists), manual AT + production deploy/backup/observability/load evidence absent. **Awaiting authorized human sign-off.** DRAGON-17b complete + committed (`45272f1`). Left unmarked pending the focused reviewer pass; not committed.
- Blockers: **local/test RC = none.** Staging/production/final-acceptance blocked by conditions C1–C5 in `RELEASE_DECISION.md` (disposition 362 rows; AT certification; deploy/backup/observability rehearsal; production load; human sign-off). The long-tracked `teams.spec.ts:58` intermittent (C6/R-FLAKE) is **closed by DRAGON-29A** — it was a stale-toast race in the test, not a flake, and is fixed.
