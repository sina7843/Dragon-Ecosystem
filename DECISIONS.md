# Project Decisions

Use this file only for material implementation clarifications not already settled in `IMPLEMENTATION_DECISIONS.md`.

## Authority order

1. `IMPLEMENTATION_DECISIONS.md`
2. `Requirements.md`
3. The active staged prompt in `prompts/`
4. Existing repository conventions and tests
5. A small documented assumption, recorded in this file

## Status of approved decisions

Every decision in `IMPLEMENTATION_DECISIONS.md` (DEC-001 … DEC-050) is treated as resolved and none of them blocks implementation. The ones DRAGON-00 acted on directly are DEC-038 (no application-managed backup; persistence and restart evidence still required), DEC-039 and DEC-047 (MongoDB 8.x with Docker Compose, Vue 3 + Vite + TypeScript, Node.js LTS + TypeScript), and DEC-040 and DEC-041 (payment and SMS remain deterministic in-repository mocks, not yet implemented). The remainder constrain later prompts and are recorded in `REQUIREMENTS_TRACEABILITY.md` as they are delivered.

The only open decision touching DRAGON-00 is OD-026; its impact is recorded below.

| Date | Decision | Reason | Affected requirements/prompts |
|---|---|---|---|
| 2026-07-21 | Workspace is npm workspaces with `apps/web` and `apps/api` sharing one root `package-lock.json`. | Deterministic installs from a single lockfile; no extra package-manager dependency. | DRAGON-00 |
| 2026-07-21 | Tests use the built-in `node:test` runner with native TypeScript type stripping; no unit-test framework is added. | The standard library covers the current need; avoids a dependency the repository does not yet require. | TEST-001, DRAGON-00 |
| 2026-07-21 | Locale is a route prefix (`/fa`, `/en`) rather than negotiated routing. | Section 9.1 permits either; a prefix makes direct URL refresh and browser tests deterministic. | I18N-002, TEST-017, DRAGON-00 |
| 2026-07-21 | OD-026 remains unresolved: no analytics or error-monitoring adapter, code, or environment variable exists. | Gated behaviour stays out of scope rather than shipped disabled, so no false integration is presented. | OD-026, INT-007, ANALYTICS-001 |
| 2026-07-21 | `/health` implements liveness only; no readiness endpoint yet. | Section 28.4 limits liveness to process viability, and the API has no dependency to probe until the data layer lands. | DRAGON-01 |
| 2026-07-21 | The API validates `MONGODB_URI` but does not open a MongoDB connection. | DRAGON-00 establishes the service and network boundary; the driver and data model belong to DRAGON-01. | DEC-039, DRAGON-01 |
| 2026-07-21 | API error messages are English at this stage; the envelope carries a stable machine-readable `code`. | Localized API messaging depends on the shared localization layer delivered by DRAGON-02. | API section 15.2, I18N-012, DRAGON-02 |
| 2026-07-21 | Browser tests run on Chromium only. | The full browser and viewport matrix in section 31 is owned by DRAGON-16a; running one real browser satisfies the DRAGON-00 acceptance criterion. | TEST-012, DRAGON-16a |
| 2026-07-21 | The web container uses `nginx-unprivileged` on port 8080. | Satisfies the nonroot container requirement without a custom user setup. | Section 34.2, DRAGON-00 |
| 2026-07-21 | MongoDB runs as a single-node replica set (`rs0`) in the default Compose stack. | MongoDB provides multi-document transactions only on a replica set, and section 32.1 requires atomic transactions. Host port 27017 remains unpublished. | Section 32.1, DEC-039, DRAGON-01 |
| 2026-07-21 | Integration tests use a separate `docker-compose.test.yml` database published on `127.0.0.1:27018` only. **Approved by the repository owner on 2026-07-21** under `IMPLEMENTATION_DECISIONS.md` section 8, conditional on loopback-only binding. | The default Compose file must never publish 27017 and does not. The override is test-only, in-memory, and disposable; binding to loopback keeps it unreachable from the network. Production networking is unchanged. | IMPLEMENTATION_DECISIONS section 8, TEST-003, DRAGON-01 |
| 2026-07-21 | Runtime validation and OpenAPI generation share one JSON Schema source through Fastify and `@fastify/swagger`. | The published contract cannot drift from the contract the server enforces, and no separate schema language is introduced. | Section 32.2, DOC-008, DRAGON-01 |
| 2026-07-21 | Every domain write goes through `runUnitOfWork`, which flushes audit rows and outbox events inside the write transaction. | Makes "no feature bypasses audit correlation or the repository boundary" a structural property rather than a review reminder. | EVENT-001, AUDIT-002, DRAGON-01 |
| 2026-07-21 | Module dependency rules are enforced by ESLint `no-restricted-imports` zones. | A documented boundary that nothing checks is a boundary that erodes; this fails the lint gate instead. | Section 32.1, DRAGON-01 |
| 2026-07-21 | Money uses JavaScript safe integers with explicit guards, not `bigint` or a decimal library. | Rial amounts at the approved scale stay far inside the safe-integer range, and every constructor rejects unsafe or fractional input. Revisit if an asset ever needs sub-unit scale. | CON-002, DATA-061, DRAGON-01 |
| 2026-07-21 | A failed idempotent attempt deletes its reservation so the key can be retried. | A provider timeout must not permanently burn a client's idempotency key; the alternative is a stuck key requiring manual clearing. | Section 15.1, SEC-020, DRAGON-01 |
| 2026-07-21 | Database sources avoid TypeScript parameter properties and other non-strippable syntax. | Node runs these sources directly via native type stripping for tests and development; parameter properties fail at load time. | DRAGON-01 |
| 2026-07-21 | Component behaviour is covered by Playwright across representative viewports rather than a separate component-test framework. | Playwright is already installed and exercises the real production build in both locales; adding a second framework and a DOM emulator would duplicate coverage. Pure logic keeps `node:test` unit tests. Revisit if a component ever needs isolated mounting that a route cannot provide. | TEST-002, TEST-012, DRAGON-02 |
| 2026-07-21 | Design-token contrast is verified by a test that parses `tokens.css` itself. | A hardcoded copy of the palette in the test would drift from the stylesheet; parsing the source makes the WCAG AA claim reproducible. | A11Y-008, section 23.2, DRAGON-02 |
| 2026-07-21 | One `AppShell` component serves the public, account, and administration areas via a `variant` prop. | The three areas differ only in navigation set and density; three near-identical shell files would triplicate the chrome and its accessibility wiring. | Section 9.4, DRAGON-02 |
| 2026-07-21 | Dialogs use the native `<dialog>` element. | Focus trapping, Escape handling, focus restoration, and background inertness come from the platform instead of hand-rolled JavaScript that would need its own tests. | A11Y-006, DRAGON-02 |
| 2026-07-21 | Pagination uses text labels rather than arrow glyphs. | Arrows are directional and would need mirroring per locale; page numbers keep the same meaning in both directions. | Section 9.4, section 17.5, DRAGON-02 |
| 2026-07-21 | The Toman conversion constant is duplicated in `apps/web/src/i18n/format.ts` for display. | Web and API are separate workspaces and the API contract is authoritative; a shared package for one constant is not yet justified. Both sides assert the same 10 rial rule in tests. | CON-002, DEC-020, DRAGON-02 |
| 2026-07-21 | On viewports below 768px the notification region docks to the top of the screen. | Found by a browser test: at the bottom it covered the primary action area and intercepted clicks, violating the rule that sticky elements must not obscure content or focus. | Section 22.2, DRAGON-02 |
| 2026-07-21 | A non-indexable `/{locale}/design-system` route ships with the application. | The shared components and states need a real place to live and be tested; it is a developer reference, clearly labelled, with sample data marked as sample, not a product feature placeholder. | Section 32.2, TEST-002, DRAGON-02 |
| 2026-07-22 | An account is created at the first successful OTP verification, in state `active`. | The mobile identity is verified at that moment, so `pending_verification` would be immediately stale. The state remains in the machine for flows that create accounts before verification; no such flow exists yet. | AUTH-009, section 12.3, DRAGON-03 |
| 2026-07-22 | The mock SMS delivery record never contains the OTP code; the code is written only to a separate development inbox that is not populated when `NODE_ENV=production`. | AUTH-003 forbids reversible OTP storage. The approved mock is the current provider, so its records must meet the same bar as a real one. | AUTH-003, SEC-012, DEC-041, DRAGON-03 |
| 2026-07-22 | The development SMS inbox route is not registered at all outside development and test, rather than being permission-guarded. | A route that does not exist cannot be reached by any caller, which is a stronger control than an authorization check. | DEC-041, DRAGON-03 |
| 2026-07-22 | The session cookie sets `Secure` only when `NODE_ENV=production`. | TLS is mandatory in production, where the flag matters. Development and automated test run over plain HTTP on loopback, where a `Secure` cookie is never returned and the suite could not exercise authenticated journeys. | SEC-001, section 16.1, DRAGON-03 |
| 2026-07-22 | `SameSite=Lax` is the current CSRF control for the session cookie. | It blocks cross-site form posts, which covers the mutating endpoints that exist today. A token-based scheme is owned by DRAGON-16b. | SEC-006, DRAGON-16b |
| 2026-07-22 | OD-028 remains unresolved, so username policy is format and uniqueness only. | No reserved-name list and no change-frequency limit is invented. Both are additive once the policy is approved. | OD-028, FORM-003, DRAGON-03 |
| 2026-07-22 | OD-003 remains unresolved: the email adapter exists but delivery is disabled and no email field is collected. | Keeping the boundary without a provider avoids presenting a verification flow that cannot complete. | OD-003, AUTH-005, DRAGON-03 |
| 2026-07-22 | OD-029 remains unresolved: no account-recovery path is implemented. | Recovery without mobile or email stays disabled; nothing partial is exposed. | OD-029, AUTH-008, DRAGON-03 |
| 2026-07-22 | Server error codes are mapped to localized messages in the browser rather than localizing API responses. | The API returns stable machine-readable codes; wording lives in the locale bundles, so no server string reaches a user and the contract stays language-neutral. | Section 13.1, section 15.2, I18N-012, DRAGON-03 |
| 2026-07-22 | Locale keys use underscores where the underlying code contains a dot, for example `security.eventType.session_created`. | vue-i18n treats a dot as a path separator, so a literal dotted key silently resolves to a missing translation. | I18N-009, DRAGON-03 |
| 2026-07-22 | Fastify `trustProxy` is scoped to nginx's fixed Compose address via `TRUSTED_PROXIES`, never `true` and never a hop count; the default stack no longer publishes the API host port. | Security review found the per-IP OTP rate limit collapsed to one shared bucket behind the proxy (a cheap platform-wide OTP-login DoS) because `request.ip` was the proxy address. Trusting only nginx restores per-client keying; removing the host port removes the only path a direct caller could use to spoof `X-Forwarded-For`. `true` or a hop count would re-open the spoof once the API is reachable directly. | SEC-009, DRAGON-03 |
| 2026-07-22 | nginx holds a static Compose address (`172.28.0.10`) on a fixed subnet; other services get dynamic addresses and are not trusted. | The API must trust exactly one proxy address. A dynamic nginx IP could not be named in `TRUSTED_PROXIES`; trusting the whole subnet would trust every sibling container. | SEC-009, DRAGON-03 |
| 2026-07-22 | Authorization is deny-by-default: a permission a role does not hold is denied, and the policy evaluator (`shared/authz/policy.ts`) is pure so the matrix is directly testable. | CON-006 and section 16.2 require server-enforced least privilege. A pure evaluator keeps the security decision out of route glue and unit-testable for IDOR and escalation. | CON-006, section 16.2, ADMIN-001, DRAGON-04 |
| 2026-07-22 | Role→permission mapping is code-owned (`shared/authz/permissions.ts`) and the seed rewrites `role_definitions.permissions` from it on every run. | Admins assign roles to users, not permissions to roles (section 6.2). A permission change ships as code + redeploy, not a data edit, so the catalogue cannot drift. This reverses the DRAGON-01 `$setOnInsert` choice. | section 6.2, DATA-006, DRAGON-04 |
| 2026-07-22 | `super_administrator` holds the wildcard permission — the single explicitly controlled unrestricted role — and its mutations are flagged `emergency` and surface in an oversight queue. | CON-006 permits one controlled super-admin; ADMIN-010 and MOD-009 require enhanced audit and retrospective review of emergency actions. | CON-006, ADMIN-010, MOD-009, DRAGON-04 |
| 2026-07-22 | Role assignments carry an optional resource scope; a scoped grant authorizes only its own resource and never a platform-wide request. | Prevents IDOR (a tournament admin for A acting on B) and privilege escalation (a scoped role unlocking global actions), section 16.2/16.4. Global grants stored with sentinel scope values so the unique index enforces one active grant per (account, role, scope). | SEC-005, section 16.4, DRAGON-04 |
| 2026-07-22 | High-risk configuration keys (`finance.`, `security.`, `payout.` prefixes) require a different actor to approve than the one who proposed; low-risk keys activate on proposal. | ADMIN-003 and FORM-021 require dual control with no self-approval for high-risk finance/security changes; low-risk config does not warrant the ceremony. The `config.activate` permission was dropped as dead once low-risk auto-activates. | ADMIN-003, FORM-021, ADMIN-009, DRAGON-04 |
| 2026-07-22 | Only a super administrator may grant or revoke a role that carries the wildcard. | A platform administrator with `roles.assign` must not be able to escalate anyone (including itself) to super administrator. Enforced in `AdminService`, covered by the matrix tests. | section 6.2, SEC-005, DRAGON-04 |
| 2026-07-22 | Session guards were extracted to `modules/identity/session-guard.ts` and shared with administration through the identity module's public index. | Session resolution stays owned by identity (section 32.1); admin reuses it rather than duplicating cookie handling. | section 32.1, DRAGON-04 |
| 2026-07-22 | Request-shape violations (missing/short/wrong-type/unknown field) return 400 from JSON-schema validation; business-rule violations (unknown role, invalid state transition, dual-control, minimum age) return 422/403/409. | Section 29 permits 400 or 422 for validation; splitting shape from semantics keeps the contract predictable and field errors meaningful. | section 29, DRAGON-04 |
| 2026-07-22 | The audit envelope gained an `emergency` flag; audit remains append-only and every high-risk mutation writes its audit event inside the same transaction. Audit export writes its own audit event. | AUDIT-001/004/007: audit is read-only to users, covers financial/role/config/moderation changes, and records who exported what and why. | AUDIT-001, AUDIT-004, AUDIT-007, DRAGON-04 |
| 2026-07-22 | Administration navigation and controls are generated from `/api/v1/admin/capabilities` (the user's effective global permissions). | Section 9.4 requires admin nav generated from effective permissions; hiding a control is never the boundary — the server enforces every route. | section 9.4, ADMIN-001, DRAGON-04 |
| 2026-07-22 | A content item is one translation-group record holding both the fa and en translations and per-locale slugs; publication requires both locales complete. | Section 17.3 mandates translation-group records and explicit canonical/alternate relationships; CONTENT-004 blocks publication with a missing required locale. One record keeps the alternate-locale slug available for hreflang. | section 17.3, CONTENT-004, DRAGON-05 |
| 2026-07-22 | Rich content is sanitised on write with a vetted library (`sanitize-html`) against a strict allowlist; the public read path serves the already-safe HTML. | CONTENT-005 and SEC-003 require sanitisation; hand-rolled HTML sanitisation is a classic XSS-bypass source, so a maintained library is used at the trust boundary. | CONTENT-005, SEC-003, DRAGON-05 |
| 2026-07-22 | Scheduling is modelled as the `published` state with a future `publishedAt`; every public read filters `publishedAt <= now`. | A scheduled item is never visible early without needing a separate visibility job; the notification/index job at publish time is deferred to DRAGON-13/15. | CONTENT-006, DRAGON-05 |
| 2026-07-22 | Content slugs are localized and unique per (type, locale); games use one slug shared across locales. | Section 17.3 allows localized slugs to differ (editorial content), while a game's identity is typically one brand name; hreflang uses each content locale's own slug. | section 17.3, SEO-003, DRAGON-05 |
| 2026-07-22 | Content and games reference a cover image by a validated URL (site path or https) only; the full media upload/scan/derivative pipeline is deferred to DRAGON-15. | MEDIA-001..015 (upload signature validation, scanning, responsive derivatives) is a large slice of its own; DRAGON-05 needs a safe media reference, not the library. Records the deferral. | MEDIA-008, CONTENT-009, DRAGON-15 |
| 2026-07-22 | Content authoring is split into `content.write` (author/editor drafts) and `content.publish` (publisher only); games use `games.manage`. | ROLE-016/017/018 separate drafting from publishing; the publish/archive transitions require the publish permission, enforced server-side. | ROLE-016, ROLE-017, ROLE-018, DRAGON-05 |
| 2026-07-22 | A development-only `POST /api/v1/dev/grant-role` (gated non-production, routed through the audited `assignRole`) exists so browser tests can create an administrator. | Mirrors the dev SMS-inbox pattern: not registered in production, so it is unreachable there; the positive admin publish journey needs a real browser path. | DRAGON-05 |
| 2026-07-22 | Team authority is resource-scoped and derived from a per-membership `role` (owner/member), not a fixed team-role column on the team document or an entry in the global RBAC catalogue. | TEAM-012 and section 16 require resource-scoped team permissions; keeping the role on each membership makes owner-only enforcement a lookup against the actor's active membership in that specific team (closing IDOR and escalation) and lets Phase 4 delegation add roles additively without a schema rewrite (TEAM-011). | TEAM-002, TEAM-011, TEAM-012, section 16.2, DRAGON-06 |
| 2026-07-22 | The team invariants — one active membership per (team, account), exactly one active owner per team, one pending invitation per (team, account) — are enforced by partial unique indexes evaluated inside the write transaction, not by read-before-write. | TEAM-003/005 must hold under concurrency; a read-then-write check races. The database is the single authority, so concurrent accepts collapse to one membership and concurrent ownership transfers leave exactly one owner. Ownership transfer demotes then promotes within one transaction under the one-active-owner index. | TEAM-003, TEAM-005, TEAM-007, DRAGON-06 |
| 2026-07-22 | Phase 1 does not lock rosters; instead every membership carries effective `joinedAt`/`leftAt` dates and roster snapshots are append-only and immutable. | DEC-013 defers roster locking, and ASM-004/BR-007 require that no membership change ever alters submitted history. Effective dates reproduce the roster at any instant, and snapshots are inserted, never updated or deleted, ready for team tournament registration and match history. | DEC-013, ASM-004, BR-007, TEAM-010, DRAGON-06 |
| 2026-07-22 | Team invitations target a player by username (the public handle), never by mobile number. | The username is already the public identifier; inviting by mobile would turn the invite endpoint into a mobile-number oracle. Invitations addressed to another account return 404, not 403, so they cannot be enumerated. | TEAM-004, section 16.4, DRAGON-06 |
| 2026-07-22 | A team is private by default; the public team page and public gaming-identity view expose only members and identities whose owning profile is itself public. | Privacy by default (DEC-043) extends to team membership: a private-profile member is not listed on a public team, and a private profile hides its gaming identities entirely (a real 404). | DEC-043, TEAM-001, DRAGON-06 |
| 2026-07-22 | A team's slug is stable: renaming a team never changes its slug unless a new slug is explicitly supplied. | The slug is the public URL; regenerating it from the name on every rename would silently break existing public links. Matches the games module's slug handling. | section 5.9, TEAM-001, DRAGON-06 |
| 2026-07-22 | DRAGON-07 implements the tournament *definition* and public discovery only; registration, matches, brackets, and payment execution are deferred to DRAGON-08/09/10. | The prompt's acceptance criteria scope this slice to authoring, fees/prizes configuration, rule profiles, and discovery. Keeping the definition self-contained avoids pulling registration and competition state into an authoring change. | TOURN-001, TOURN-002, DRAGON-07 |
| 2026-07-22 | OD-006 is handled as a feature-gate: no approved game/publisher/federation rule profiles ship, so tournaments use a custom free-text rule profile (always publishable) and named external profiles stay out of scope. | The gate's fallback is "affected games/tournaments remain draft and unpublishable." Reading "rule profile" as satisfiable by custom rules keeps publication demonstrable while withholding the gated named-profile feature until OD-006 resolves. | OD-006, TOURN-002, DRAGON-07 |
| 2026-07-22 | Tournament entry fees and prizes are stored as exact integer `Money`: Toman as rial (×10) and Dragon Coin as whole units, with a per-amount domain ceiling so a conversion can never overflow the safe-integer range. No payment is executed in this prompt. | CON-002 forbids floating-point money; TOURN-012 requires server-calculated fee components to equal the configured price exactly. The ceiling turns an oversized amount into a clean 422 rather than an opaque 500. | CON-002, DEC-020, TOURN-012, DRAGON-07 |
| 2026-07-22 | Prize definitions and custom-question sets each carry their own version counter that increments only when the set changes; the tournament's own version bumps on every edit and each change appends an immutable revision. | Versioned prizes and TOURN-007 versioned answers require a stable version to stamp; bumping only on real change keeps versions meaningful, while the revision log preserves full history (TOURN-026). | TOURN-007, TOURN-026, DRAGON-07 |
| 2026-07-22 | Tournament authoring is gated on the existing global `tournament.manage` permission (any holder manages any tournament), matching the content and games modules; resource-scoped organizer/referee ownership is deferred to DRAGON-08. | Consistency with the established admin-authoring pattern; resource-scoped tournament assignments (TOURN-018) belong with tournament operations, not definition. Global gating is not an IDOR regression relative to content/games. | ROLE-008, TOURN-018, section 16, DRAGON-07 |
| 2026-07-22 | Publication validation aggregates every blocker — missing bilingual values and any date-ordering violation — into one response, and cross-field date ordering (registration opens < closes ≤ start < end) is also enforced on every save. | TOURN-002 requires publication validation to report each missing mandatory value; aggregating avoids a fix-one-error-at-a-time loop. Save-time ordering means a stored draft never holds misordered dates. | TOURN-002, DRAGON-07 |
| 2026-07-22 | Tournament capacity is enforced by an atomic conditional increment on a per-tournament seat counter (main count below the frozen published capacity) inside the registration transaction. | TOURN-005 requires final-slot correctness under concurrency; a read-then-write check races. A single counter document serializes concurrent claims so it can never overbook, and a rolled-back transaction (duplicate key, or full-with-no-waitlist) rolls back the seat claim too. Capacity is frozen because it can only be edited while the tournament is a draft. | TOURN-005, section 15.1, DRAGON-08 |
| 2026-07-22 | Exactly one active registration per participant is guaranteed by a partial unique index on (tournamentId, subjectId) where active is true; the active flag derives from the registration state via a single predicate (pending/approved/waitlisted). | TOURN-009 requires duplicate prevention with concurrent duplicates producing one active entry and one conflict; deriving the flag from one predicate keeps the index and the state machine from drifting, so a rejected/cancelled participant may re-register while a live one cannot. | TOURN-009, DRAGON-08 |
| 2026-07-22 | Registration submission is wrapped in the shared idempotency helper (scope registration:tournamentId, client-supplied key); waitlist positions come from a monotonic increment on the seat counter. | TOURN-013 requires idempotent submission (a replay returns the stored registration, not a duplicate); TOURN-006 requires deterministic, auditable waitlist ordering. Known limitation: the idempotency completion write is not inside the registration transaction, so a crash in a narrow window can strand a key until its TTL — without overbooking or duplication, since the counter and the active-unique index remain authoritative. | TOURN-006, TOURN-013, DRAGON-08 |
| 2026-07-22 | Only an approved registration occupies a seat; automatic approval claims a seat at registration time, manual approval at approval time, and promotion is an approval of a waitlisted entry. Cancelling or rejecting an approved entry releases its seat, which an administrator then fills by promotion — there is no automatic promotion. | Aligns capacity accounting with the approval mode and keeps promotion administrator-managed (TOURN-004/006/014); a released seat is a deliberate admin decision, not an implicit reshuffle. | TOURN-004, TOURN-006, TOURN-014, DRAGON-08 |
| 2026-07-22 | Team registration is performed only by the team owner and captures an immutable roster snapshot, reusing the teams module's owner-gated snapshot service; the snapshot is taken before the registration transaction, so a failed registration can leave a harmless orphan snapshot. | TOURN-009 makes the owner the team's representative and TOURN-010 requires an immutable roster snapshot that later membership changes cannot alter; reusing the teams boundary keeps section-32.1 ownership intact. Orphan snapshots are immutable and unreferenced, so they are acceptable rather than worth a cross-module transaction. | TOURN-009, TOURN-010, ASM-005, DRAGON-08 |
| 2026-07-22 | Administrative registration routes are nested under /admin/tournaments/:id/registrations and gated on tournament.manage scoped to that tournament id; decision handlers look up the registration by (registrationId, tournamentId). | TOURN-018 requires resource-scoped staff access; scoping the gate to the path tournament means a resource-scoped organizer/referee operates only their own tournament and cannot reach another's registrations (IDOR closed), while a global grant still applies everywhere. | TOURN-018, section 16.4, DRAGON-08 |
| 2026-07-22 | Registration is limited to free tournaments; a tournament with a non-free fee rejects registration with a PAYMENT_NOT_AVAILABLE error. | OD-007 keeps paid refund-trigger transitions disabled and DRAGON-08 scopes tests to free registration paths; paid checkout and refunds arrive with DRAGON-11/12. | OD-007, TOURN-012, DRAGON-08 |
| 2026-07-22 | The competition engine (seeding, single-elimination generation, round-robin scheduling, byes) is a pure module with no I/O, clock, or RNG; match structure uses logical position keys and the persistence layer assigns stable opaque ids. | BRACKET-016/018 require deterministic, reproducible generation for the same validated input and seed and a common competition interface; a pure core makes the invariants directly property-testable, and separating structure from opaque ids keeps determinism without deriving ids from business data (DATA-001). | BRACKET-001, BRACKET-016, BRACKET-018, DRAGON-09a |
| 2026-07-22 | Deterministic seeding uses a caller-supplied manual order when present (validated as a permutation), otherwise a stable sort by a keyed SHA-256 of the seed and participant id. | BRACKET-008 requires manual ordering and reproducible automated seeding; a keyed hash is reproducible from the same inputs, independent of incoming order, and uses no floating-point or randomness. | BRACKET-008, DRAGON-09a |
| 2026-07-22 | DRAGON-09a implements single elimination and round robin only; double elimination, Swiss, and manual/custom are validated as unsupported and delivered by DRAGON-09b. Round robin is bounded to 64 participants (synchronous generation); the 1,000-limit round robin via a background job is DRAGON-09c. | The slice scope is the two initial formats with proportional verification; generating a 1,000-participant round robin (about half a million matches) synchronously is a background-job concern deferred to the standings/concurrency slice. | BRACKET-003, DEC-046, DRAGON-09a |
| 2026-07-22 | One competition per tournament is enforced by a unique index on tournamentId; a result is recorded under an optimistic (version, state:ready) guard; every state change goes through runUnitOfWork with an audit event and an outbox event. | The parent prompt requires preventing duplicate competition state, impossible/duplicate progression, and stale writes; a unique index makes concurrent generation collapse to one, and the version+state guard makes concurrent progression yield one success and one conflict with a result applied at most once. Competition generation is built from approved registrations only (the authoritative source) and carries each team entry's immutable roster snapshot. | BRACKET-010, BRACKET-011, BRACKET-016, TOURN-010, DRAGON-09a |
| 2026-07-22 | Double elimination ships a SINGLE deciding grand final; the bracket-reset variant (the losers-bracket champion must beat the winners-bracket champion twice) is gated behind an unresolved decision, not invented. | BRACKET-002/007 make grand-final behavior a rule-profile concern, and OD-006 keeps approved rule profiles gated. The winners/losers routing and second-loss elimination are structural and unambiguous; only the grand-final reset is disputed, so it is stopped behind an explicit gate. Consequence under the single-final default: a winners-bracket champion who loses the grand final is the runner-up with one loss. | BRACKET-002, BRACKET-007, OD-006, DRAGON-09b |
| 2026-07-22 | Swiss pairing uses a documented default policy: score groups by accepted-result wins, ordered by score then seed, paired by a deterministic backtracking matching that avoids prior pairings when a rematch-free perfect matching exists and falls back deterministically otherwise; an odd count byes the fewest-byes/lowest-score/lowest-seed player, and a bye counts as a win. The exact bye-scoring and tiebreak policy is a gated rule-profile concern. | BRACKET-004/007 route Swiss pairing and bye scoring to versioned profiles, which OD-006 gates; the default is a standard, non-federation-specific mechanic, and the specific scoring is preserved as an explicit gate rather than invented. | BRACKET-004, BRACKET-007, OD-006, DRAGON-09b |
| 2026-07-22 | Manual/custom competitions are a validated declarative graph (data, never executable expressions), acyclic with unique keys, each slot seeded XOR written by exactly one upstream transition, no conflicting writers, no orphan slots, and each participant seed used at most once across the graph. | BRACKET-005 requires staff-defined matches and progression without automated bracket assumptions; OD-006 forbids executable custom rules. Validating the graph up front guarantees results only ever propagate inside it, and seeding each participant once prevents double-booking and downstream self-play. Group/round-robin-style manual graphs with repeated participants are deferred. | BRACKET-005, OD-006, DRAGON-09b |
| 2026-07-22 | Advanced formats are bounded for synchronous generation: single elimination ≤1000, double elimination ≤256, round robin ≤64, Swiss ≤16 (backtracking matching), manual ≤256 fixtures and depth ≤32; a unique (competitionId, logical key) index enforces unique fixture keys and collapses concurrent Swiss-round or duplicate generation to one. | The slice generates synchronously; large-scale (1,000) generation via background jobs is DRAGON-09c. The unique key index is the database authority for "one Swiss round per number" and "unique logical fixture keys" without read-before-write. | BRACKET-004, BRACKET-011, DEC-046, DRAGON-09b |
| 2026-07-22 | Standings are a deterministic projection ranked by competition ranking (1, 2, 2, 4): tied participants share a rank and the seeded participant order is the sole stable final fallback (never the wall clock, insertion order, or Mongo natural order). Only accepted (completed/bye) results count; the points policy is win = 1 point (versioned); round-robin/Swiss are points-based and single/double elimination are placement-based (champion, runner-up, eliminated-by-round); manual returns a partial wins-derived ordering. | BRACKET-015 requires reproducible standings recording; a pure calculator with a seeded fallback guarantees byte-equivalent ordered rows for identical inputs. Opponent-based Swiss tiebreaks (Buchholz), draw support, and named points policies are rule-profile concerns kept gated by OD-006. | BRACKET-015, OD-006, DRAGON-09c |
| 2026-07-22 | Standings are published as versioned append-only snapshots with exactly one current per competition (unique partial index) and one per calculation version; recalculation runs inside the mutating transaction under an optimistic standingsVersion guard. | The parent requires one current projection per competition/version, no stale overwrite, and deterministic reconciliation. Recomputing inside the same transaction as the result/correction keeps standings atomically consistent with accepted results; the version guard plus unique indexes make concurrent recalculation collapse to one. | BRACKET-015, BRACKET-016, DRAGON-09c |
| 2026-07-22 | A completed result may be corrected under an expected-version guard; the original is preserved as an immutable append-only revision, and the correction, its audit event, and the recalculated standings commit atomically. A correction is refused (DOWNSTREAM_HISTORY_CONFLICT) when a downstream fixture is already completed, re-checked inside the correction transaction; an allowed correction only re-points a not-yet-completed downstream slot and never rewrites completed history. | TOURN-022 requires versioned, auditable corrections with queryable history; the downstream-boundary rule prevents silently invalidating played fixtures. The in-transaction re-check closes a TOCTOU race where a downstream fixture completes between the boundary check and the correction. | TOURN-022, BRACKET-016, DRAGON-09c |
| 2026-07-22 | Competition lock lifecycle is open → correction_limited → locked, an optimistic lockVersion-guarded audited transition; a locked competition rejects result entry and corrections (re-checked inside their transactions), and no rollback deletes immutable result or audit history. Unlock is an explicit audited transition back. | BRACKET-012 requires locking to prevent mutation of an active version; optimistic concurrency makes concurrent lock transitions yield exactly one accepted change, and the in-transaction re-check makes "locked means locked" hold against a concurrent write. | BRACKET-012, DRAGON-09c |
| 2026-07-22 | Standings recalculation is synchronous within the approved size limits; bracket and admin reads are keyset-paginated over (round, index), standings is a single bounded snapshot document, and participants are identified by seed number. A background dispatcher for above-limit work is DRAGON-14. | The parent requires load-safe behavior without unbounded operations; within the documented synchronous limits a background job is unnecessary, and pagination plus a single-document projection keep reads bounded and index-backed. | BRACKET-015, DEC-046, DRAGON-09c |
