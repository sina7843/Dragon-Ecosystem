# Project Decisions

Use this file only for material implementation clarifications not already settled in `IMPLEMENTATION_DECISIONS.md`.

## Authority order

1. `IMPLEMENTATION_DECISIONS.md`
2. `Requirements.md`
3. The active staged prompt in `prompts/`
4. Existing repository conventions and tests
5. A small documented assumption, recorded in this file

## Status of approved decisions

Every decision in `IMPLEMENTATION_DECISIONS.md` (DEC-001 … DEC-050) is treated as resolved and none of them blocks implementation. The ones DRAGON-00 acted on directly are DEC-038 (no application-managed backup; persistence and restart evidence still required), DEC-039 and DEC-047 (MongoDB 8.x with Docker Compose, Vue 3 + Vite + TypeScript, Node.js LTS + TypeScript), and DEC-040 and DEC-041 (payment and SMS remain deterministic in-repository mocks, not yet implemented). The remainder constrain later prompts and are recorded in `REQUIREMENTS_TRACEABILITY.md` as they are delivered.

The only open decision touching DRAGON-00 is OD-026; its impact is recorded below.

| Date | Decision | Reason | Affected requirements/prompts |
|---|---|---|---|
| 2026-07-21 | Workspace is npm workspaces with `apps/web` and `apps/api` sharing one root `package-lock.json`. | Deterministic installs from a single lockfile; no extra package-manager dependency. | DRAGON-00 |
| 2026-07-21 | Tests use the built-in `node:test` runner with native TypeScript type stripping; no unit-test framework is added. | The standard library covers the current need; avoids a dependency the repository does not yet require. | TEST-001, DRAGON-00 |
| 2026-07-21 | Locale is a route prefix (`/fa`, `/en`) rather than negotiated routing. | Section 9.1 permits either; a prefix makes direct URL refresh and browser tests deterministic. | I18N-002, TEST-017, DRAGON-00 |
| 2026-07-21 | OD-026 remains unresolved: no analytics or error-monitoring adapter, code, or environment variable exists. | Gated behaviour stays out of scope rather than shipped disabled, so no false integration is presented. | OD-026, INT-007, ANALYTICS-001 |
| 2026-07-21 | `/health` implements liveness only; no readiness endpoint yet. | Section 28.4 limits liveness to process viability, and the API has no dependency to probe until the data layer lands. | DRAGON-01 |
| 2026-07-21 | The API validates `MONGODB_URI` but does not open a MongoDB connection. | DRAGON-00 establishes the service and network boundary; the driver and data model belong to DRAGON-01. | DEC-039, DRAGON-01 |
| 2026-07-21 | API error messages are English at this stage; the envelope carries a stable machine-readable `code`. | Localized API messaging depends on the shared localization layer delivered by DRAGON-02. | API section 15.2, I18N-012, DRAGON-02 |
| 2026-07-21 | Browser tests run on Chromium only. | The full browser and viewport matrix in section 31 is owned by DRAGON-16a; running one real browser satisfies the DRAGON-00 acceptance criterion. | TEST-012, DRAGON-16a |
| 2026-07-21 | The web container uses `nginx-unprivileged` on port 8080. | Satisfies the nonroot container requirement without a custom user setup. | Section 34.2, DRAGON-00 |
| 2026-07-21 | MongoDB runs as a single-node replica set (`rs0`) in the default Compose stack. | MongoDB provides multi-document transactions only on a replica set, and section 32.1 requires atomic transactions. Host port 27017 remains unpublished. | Section 32.1, DEC-039, DRAGON-01 |
| 2026-07-21 | Integration tests use a separate `docker-compose.test.yml` database published on `127.0.0.1:27018` only. **Approved by the repository owner on 2026-07-21** under `IMPLEMENTATION_DECISIONS.md` section 8, conditional on loopback-only binding. | The default Compose file must never publish 27017 and does not. The override is test-only, in-memory, and disposable; binding to loopback keeps it unreachable from the network. Production networking is unchanged. | IMPLEMENTATION_DECISIONS section 8, TEST-003, DRAGON-01 |
| 2026-07-21 | Runtime validation and OpenAPI generation share one JSON Schema source through Fastify and `@fastify/swagger`. | The published contract cannot drift from the contract the server enforces, and no separate schema language is introduced. | Section 32.2, DOC-008, DRAGON-01 |
| 2026-07-21 | Every domain write goes through `runUnitOfWork`, which flushes audit rows and outbox events inside the write transaction. | Makes "no feature bypasses audit correlation or the repository boundary" a structural property rather than a review reminder. | EVENT-001, AUDIT-002, DRAGON-01 |
| 2026-07-21 | Module dependency rules are enforced by ESLint `no-restricted-imports` zones. | A documented boundary that nothing checks is a boundary that erodes; this fails the lint gate instead. | Section 32.1, DRAGON-01 |
| 2026-07-21 | Money uses JavaScript safe integers with explicit guards, not `bigint` or a decimal library. | Rial amounts at the approved scale stay far inside the safe-integer range, and every constructor rejects unsafe or fractional input. Revisit if an asset ever needs sub-unit scale. | CON-002, DATA-061, DRAGON-01 |
| 2026-07-21 | A failed idempotent attempt deletes its reservation so the key can be retried. | A provider timeout must not permanently burn a client's idempotency key; the alternative is a stuck key requiring manual clearing. | Section 15.1, SEC-020, DRAGON-01 |
| 2026-07-21 | Database sources avoid TypeScript parameter properties and other non-strippable syntax. | Node runs these sources directly via native type stripping for tests and development; parameter properties fail at load time. | DRAGON-01 |
| 2026-07-21 | Component behaviour is covered by Playwright across representative viewports rather than a separate component-test framework. | Playwright is already installed and exercises the real production build in both locales; adding a second framework and a DOM emulator would duplicate coverage. Pure logic keeps `node:test` unit tests. Revisit if a component ever needs isolated mounting that a route cannot provide. | TEST-002, TEST-012, DRAGON-02 |
| 2026-07-21 | Design-token contrast is verified by a test that parses `tokens.css` itself. | A hardcoded copy of the palette in the test would drift from the stylesheet; parsing the source makes the WCAG AA claim reproducible. | A11Y-008, section 23.2, DRAGON-02 |
| 2026-07-21 | One `AppShell` component serves the public, account, and administration areas via a `variant` prop. | The three areas differ only in navigation set and density; three near-identical shell files would triplicate the chrome and its accessibility wiring. | Section 9.4, DRAGON-02 |
| 2026-07-21 | Dialogs use the native `<dialog>` element. | Focus trapping, Escape handling, focus restoration, and background inertness come from the platform instead of hand-rolled JavaScript that would need its own tests. | A11Y-006, DRAGON-02 |
| 2026-07-21 | Pagination uses text labels rather than arrow glyphs. | Arrows are directional and would need mirroring per locale; page numbers keep the same meaning in both directions. | Section 9.4, section 17.5, DRAGON-02 |
| 2026-07-21 | The Toman conversion constant is duplicated in `apps/web/src/i18n/format.ts` for display. | Web and API are separate workspaces and the API contract is authoritative; a shared package for one constant is not yet justified. Both sides assert the same 10 rial rule in tests. | CON-002, DEC-020, DRAGON-02 |
| 2026-07-21 | On viewports below 768px the notification region docks to the top of the screen. | Found by a browser test: at the bottom it covered the primary action area and intercepted clicks, violating the rule that sticky elements must not obscure content or focus. | Section 22.2, DRAGON-02 |
| 2026-07-21 | A non-indexable `/{locale}/design-system` route ships with the application. | The shared components and states need a real place to live and be tested; it is a developer reference, clearly labelled, with sample data marked as sample, not a product feature placeholder. | Section 32.2, TEST-002, DRAGON-02 |
| 2026-07-22 | An account is created at the first successful OTP verification, in state `active`. | The mobile identity is verified at that moment, so `pending_verification` would be immediately stale. The state remains in the machine for flows that create accounts before verification; no such flow exists yet. | AUTH-009, section 12.3, DRAGON-03 |
| 2026-07-22 | The mock SMS delivery record never contains the OTP code; the code is written only to a separate development inbox that is not populated when `NODE_ENV=production`. | AUTH-003 forbids reversible OTP storage. The approved mock is the current provider, so its records must meet the same bar as a real one. | AUTH-003, SEC-012, DEC-041, DRAGON-03 |
| 2026-07-22 | The development SMS inbox route is not registered at all outside development and test, rather than being permission-guarded. | A route that does not exist cannot be reached by any caller, which is a stronger control than an authorization check. | DEC-041, DRAGON-03 |
| 2026-07-22 | The session cookie sets `Secure` only when `NODE_ENV=production`. | TLS is mandatory in production, where the flag matters. Development and automated test run over plain HTTP on loopback, where a `Secure` cookie is never returned and the suite could not exercise authenticated journeys. | SEC-001, section 16.1, DRAGON-03 |
| 2026-07-22 | `SameSite=Lax` is the current CSRF control for the session cookie. | It blocks cross-site form posts, which covers the mutating endpoints that exist today. A token-based scheme is owned by DRAGON-16b. | SEC-006, DRAGON-16b |
| 2026-07-22 | OD-028 remains unresolved, so username policy is format and uniqueness only. | No reserved-name list and no change-frequency limit is invented. Both are additive once the policy is approved. | OD-028, FORM-003, DRAGON-03 |
| 2026-07-22 | OD-003 remains unresolved: the email adapter exists but delivery is disabled and no email field is collected. | Keeping the boundary without a provider avoids presenting a verification flow that cannot complete. | OD-003, AUTH-005, DRAGON-03 |
| 2026-07-22 | OD-029 remains unresolved: no account-recovery path is implemented. | Recovery without mobile or email stays disabled; nothing partial is exposed. | OD-029, AUTH-008, DRAGON-03 |
| 2026-07-22 | Server error codes are mapped to localized messages in the browser rather than localizing API responses. | The API returns stable machine-readable codes; wording lives in the locale bundles, so no server string reaches a user and the contract stays language-neutral. | Section 13.1, section 15.2, I18N-012, DRAGON-03 |
| 2026-07-22 | Locale keys use underscores where the underlying code contains a dot, for example `security.eventType.session_created`. | vue-i18n treats a dot as a path separator, so a literal dotted key silently resolves to a missing translation. | I18N-009, DRAGON-03 |
| 2026-07-22 | Fastify `trustProxy` is scoped to nginx's fixed Compose address via `TRUSTED_PROXIES`, never `true` and never a hop count; the default stack no longer publishes the API host port. | Security review found the per-IP OTP rate limit collapsed to one shared bucket behind the proxy (a cheap platform-wide OTP-login DoS) because `request.ip` was the proxy address. Trusting only nginx restores per-client keying; removing the host port removes the only path a direct caller could use to spoof `X-Forwarded-For`. `true` or a hop count would re-open the spoof once the API is reachable directly. | SEC-009, DRAGON-03 |
| 2026-07-22 | nginx holds a static Compose address (`172.28.0.10`) on a fixed subnet; other services get dynamic addresses and are not trusted. | The API must trust exactly one proxy address. A dynamic nginx IP could not be named in `TRUSTED_PROXIES`; trusting the whole subnet would trust every sibling container. | SEC-009, DRAGON-03 |
